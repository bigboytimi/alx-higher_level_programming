#!/usr/bin/node

const count = Math.floor(Number(process.argv[0]));
if (isNaN(size)) {
	console.log('Missing size');
} else {
	for (let i = 0; i < count; i++){
		for row = ""
	for(let j = 0; j < count; row+='X'){
		console.log(row);
	}
}
